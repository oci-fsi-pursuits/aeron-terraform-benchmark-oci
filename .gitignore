# Terraform
.terraform/
# Commit .terraform.lock.hcl for reproducible provider versions.
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Keys and local secrets
*.pem
*.key
id_rsa*
*.p12
*.pfx

# Archives (never commit ad-hoc zips/tarballs that might hold credentials or state)
*.zip
*.tgz
*.tar.gz

# Saved Terraform plans
tfplan*
*.tfplan

# Local benchmark/debug scratch
tmp-*
.codex-*
HdrSummary.java
_remote-*-artifacts/
__pycache__/
*.pyc
aeron-*.txt
aeron-*.csv
cpg-capacity-results-*.jsonl
cpu-placement-samples-*.csv

# Local validation artifacts
validation/
validation/**/generated_key*

# Pulled driver-matrix summary for terraform output (may contain latencies / paths)
.terraform-matrix-summary.json

# Local env / generated benchmark config (may contain IPs or paths)
**/benchmark-config.env
.env
.env.*

# Test files
test.tfvars

# Generated playbooks archive (used during apply)
playbooks.zip
playbooks.tar.gz
